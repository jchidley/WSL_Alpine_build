Script to build an Alpine distribution for WSL on a windows machine. Uses
Alpine's [alpine-chroot-install script](https://github.com/alpinelinux/alpine-chroot-install)

You'll need to edit and test this yourself. I have used this on a Debian WSL host.