# pi-agent Module

Installs the pi coding agent CLI used in this environment.

## Includes
- Node.js + npm
- Global install of `@mariozechner/pi-coding-agent`

## Commands
- `install-pi-agent` to (re)install the CLI
